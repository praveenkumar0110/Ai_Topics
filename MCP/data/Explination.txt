
You type path (C:/temp/allpattern.py)
   ↓
Client send path to MCP tool
   ↓
MCP tool read file
   ↓
Return code text
   ↓
Client send code to LLM
   ↓
LLM explain

FastMCP → MCP server create panna
ClientSession → MCP client ah use panni server oda pesanum
stdio_client → same file la server & client communicate panna (terminal/stdin/stdout use panni)



step 1 
mcp = FastMCP("CodeExplainerTool")  -- CodeExplainerTool nu oru MCP server create panren


Step 2 
def read_code_file intha function job--- Given a file path → return file content

---------------------------------------------------------------------

step 3 
async def run_client():---same file clinet and server do

async with stdio_client(server_params) as (read, write):
Ithu client ↔ server connection create pannudhu using terminal pipes.

async with ClientSession(read, write) as session:
    await session.initialize()
Client session start pannudhu.

file_path = input("Enter full file path to explain: ").strip()
User kitta file path kekkudhu.
------------------------------------------------------------------------
step 4
result = await session.call_tool(
    "read_code_file",
    {"path": file_path}
)

🔥🔥 IMPORTANT LINE

Client → MCP server kitte solludhu:

“read_code_file tool ah call pannu, indha path use panni”

code_content = result.content[0].text
Server return panna file content inga varudhu.

----------------------------------------------------------------

step 5

Prompt create pannuring LLM ku
prompt = f"""
"""
LLM ku nalla structured prompt.


response = ollama.chat(
    model="llama3",
    messages=[{"role": "user", "content": prompt}]
)
Ippo than LLM call aagudhu.

File content → llama3 ku pogudhu.



print(response["message"]["content"])
LLM explanation print pannudhu.





Tips:
1) Same file irundhaalum, adhu rendu thadava run aagudhu — onnu client, onnu server.

2) Server oda only job:

“Path kudutha → file content thirupi kudu”

one  line hole Logic
| Role   | Work                                        |
| ------ | ------------------------------------------- |
| Client | Path anupudhu, LLM kitta kekkudhu           |
| Server | File read pannudhu, content thirupi kududhu |
