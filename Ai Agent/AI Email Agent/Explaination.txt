🥇 Step 1 — main la
run_agent(q)


User input → run_agent ku pogudhu.
-------------------------------------------------------------------------------
🥈 Step 2 — read_policy() run aagudhu
policy = read_policy()


Privacy policy file read pannitu text vangura.
--------------------------------------------------------------------------
🥉 Step 3 — Prompt build pannura
prompt = f""" ... {policy} ... {user_input} ... """


Policy add pannura

User request add pannura

JSON format la reply kudu nu strict ah sollura
-----------------------------------------------------------------------------------
🟡 Step 4 — ask_ollama(prompt)
output = ask_ollama(prompt)


Ithu LLM API call.

LLM JSON maari reply kudukkum (sometimes extra text um irukkum).
---------------------------------------------------------------------------------------
🟠 Step 5 — extract_json(output)
clean = extract_json(output)


LLM output la irundhu { ... } part mattum cut pannura.
---------------------------------------------------------------------------------------
🔵 Step 6 — json.loads()
data = json.loads(clean)


JSON string → Python dict.
-------------------------------------------------------------------------------------
🔥 Step 7 — Tool execution
if data["action"] == "send_email":
    send_email(...)


LLM sonnadha base panni real email anuppura.
